/**
 * Returns a new array containing only the non-negative numbers from the input array.
 * (Non-negative means zero or positive.)
 * @param {number[]} numbers - An array of numbers to be filtered.
 * @returns {number[]} A new array containing only non-negative numbers.
 */
export default function filterOutNegatives(numbers) {
  // TODO: filter out negative numbers
}
